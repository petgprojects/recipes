node_modules/
.pnpm-store/
dist/
build/
.next/
out/
*.tsbuildinfo

.env
.env.local
.env.*.local
# The DEV_AUTH_FALLBACK round-trip in HANDOFF.md parks a copy of .env here
# while the web container is recreated. It holds the same live secrets.
.env.backup

.DS_Store
*.log
npm-debug.log*
pnpm-debug.log*

coverage/
.vitest/

# local volumes / cached recipe images if ever bind-mounted
/pgdata/
/recipe-images/
# RECIPE_IMAGES_DIR when the worker runs natively instead of under compose
/data/

# Google OAuth client downloads. The values belong in .env; the file itself is
# a credential and must never be committed.
client_secret_*.json
